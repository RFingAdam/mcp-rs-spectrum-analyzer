[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "spectrum-analyzer-mcp"
version = "0.3.0"
description = "MCP server for spectrum/signal analyzer automation via SCPI (R&S, Keysight, Rigol, Siglent, Anritsu, Tektronix)"
readme = "README.md"
license = "AGPL-3.0-or-later"
requires-python = ">=3.10"
authors = [
    { name = "Adam Engelbrecht", email = "adam@rfingadam.com" }
]
keywords = [
    "mcp",
    "spectrum-analyzer",
    "scpi",
    "test-automation",
    "rf-testing",
    "emc",
    "rohde-schwarz",
    "keysight",
    "rigol",
    "siglent",
    "anritsu",
]
classifiers = [
    "Development Status :: 4 - Beta",
    "Intended Audience :: Developers",
    "Intended Audience :: Science/Research",
    "License :: OSI Approved :: GNU Affero General Public License v3 or later (AGPLv3+)",
    "Operating System :: OS Independent",
    "Programming Language :: Python :: 3",
    "Programming Language :: Python :: 3.10",
    "Programming Language :: Python :: 3.11",
    "Programming Language :: Python :: 3.12",
    "Topic :: Scientific/Engineering",
    "Topic :: Scientific/Engineering :: Electronic Design Automation (EDA)",
]
dependencies = [
    "mcp>=1.0.0,<2.0.0",
    "pydantic>=2.0",
    "pydantic-settings>=2.0",
    # PEP 508 direct reference (not a bare name): "scpi-core" also exists on
    # PyPI as an unrelated package by a different author in the same problem
    # domain, so a bare name silently resolves to the wrong library for any
    # installer that doesn't read [tool.uv.sources] (e.g. plain pip).
    "scpi-core @ git+https://github.com/RFingAdam/scpi-core.git@main",
]

[project.optional-dependencies]
# Transport-level VISA support now lives in scpi-core, so this extra defers to
# it rather than pinning pyvisa twice and risking a version split between the
# transport and the server that uses it.
visa = [
    "scpi-core[visa]",
]
sim = [
    "scpi-core[sim]",
    "pyyaml>=6.0",
]
dev = [
    "pytest>=7.0",
    "pytest-asyncio>=0.21",
    "pytest-cov>=4.0",
    "ruff>=0.1.0",
    "mypy>=1.0",
    "pyyaml>=6.0",
]

[project.scripts]
spectrum-analyzer-mcp = "spectrum_analyzer_mcp.__main__:main"
spectrum-simulator = "spectrum_analyzer_mcp.sim.__main__:main"

[project.urls]
Homepage = "https://github.com/RFingAdam/mcp-rs-spectrum-analyzer"
Repository = "https://github.com/RFingAdam/mcp-rs-spectrum-analyzer"
Issues = "https://github.com/RFingAdam/mcp-rs-spectrum-analyzer/issues"

[tool.uv.sources]
# The git URL lives in `dependencies` so pip honours it too; uv pins the
# exact commit in uv.lock. For local development across both repos, uncomment
# the path form below and do not commit it uncommented (CI has no sibling
# scpi-core checkout, so a committed path override breaks the build there).
# scpi-core = { path = "../scpi-core", editable = true }

[tool.hatch.metadata]
# scpi-core is depended on by git URL, which hatchling treats as a direct
# reference. Required because the name is taken on PyPI by an unrelated
# package -- see the note in [project.dependencies].
allow-direct-references = true

[tool.hatch.build.targets.wheel]
packages = ["src/spectrum_analyzer_mcp"]

[tool.pytest.ini_options]
asyncio_mode = "auto"
testpaths = ["tests"]

[tool.ruff]
line-length = 100
target-version = "py310"

[tool.ruff.lint]
select = ["E", "F", "I", "N", "W", "UP"]

[tool.mypy]
python_version = "3.10"
strict = true
